package com.example.hw51.data

interface SimpleCounter {
    fun showCount(count: Int)
    fun changeTextSize(textSize: Float)
    fun changeTextColor(color: Int)
    fun showToast()
    fun setVisibilityButton(visibility: Int)
}